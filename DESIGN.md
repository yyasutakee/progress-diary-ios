what i wanna make: an ios app. basically it is a list app so i can register what i progressed daily. each list item is simply text and it must have date created. each row must show that text and also the created date as the format of mm.dd e.g. 12/31. make sure each list row must be separated each other and the shape is rounded rectangle. unlike normal list apps, this app must have an overlay of heatmap on top area. u can imagine this is like github heatmap (horizontally layout from 1.1 to 12.31). and each grid cell be filled when an item is added for that day (fill is binary, so either filled or not filled).
